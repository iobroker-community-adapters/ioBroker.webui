export class DefaultEditorTypeService {
    getEditor(type, additional) {
        if (type === 'color' || type === 'css-color') {
            const input = document.createElement('node-projects-color-input');
            input.addEventListener('change', () => additional.changedCallback(input.value));
            return {
                element: input,
                getValue: () => input.value,
                setValue: (value) => { input.value = value; }
            };
        }
        const input = document.createElement('input');
        input.type = type;
        if (additional.min !== undefined)
            input.min = additional.min;
        if (additional.max !== undefined)
            input.max = additional.max;
        if (additional.step !== undefined)
            input.step = additional.step;
        input.addEventListener('change', (event) => additional.changedCallback(event.target.value));
        return {
            element: input,
            getValue: () => input.value,
            setValue: (value) => { input.value = value; }
        };
    }
}
//# sourceMappingURL=DefaultEditorTypeService.js.map