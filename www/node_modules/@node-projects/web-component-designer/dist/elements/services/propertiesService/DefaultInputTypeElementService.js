export class DefaultInputTypeElementService {
    getEditor(type, changedCallback) {
        if (type === 'color') {
            const input = document.createElement('input');
            input.type = 'color';
            input.addEventListener('change', changedCallback);
            return {
                element: input,
                getValue: () => input.value
            };
        }
        const input = document.createElement('input');
        input.type = type;
        input.addEventListener('change', changedCallback);
        return {
            element: input,
            getValue: () => input.value
        };
    }
}
//# sourceMappingURL=DefaultInputTypeElementService.js.map