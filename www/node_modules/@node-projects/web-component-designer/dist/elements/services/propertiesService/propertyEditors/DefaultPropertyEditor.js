import { BasePropertyEditor } from './BasePropertyEditor.js';
export class TextPropertyEditor extends BasePropertyEditor {
    editor;
    constructor(property, serviceContainer) {
        super(property);
        const editor = serviceContainer.forSomeServicesTillResult('editorTypeService', x => x.getEditor(property.type, {
            changedCallback: (newValue) => this._valueChanged(newValue)
        }));
        this.editor = editor;
        this.element = editor.element;
    }
    refreshValue(valueType, value) {
        this.editor.setValue(value);
    }
}
//# sourceMappingURL=DefaultPropertyEditor.js.map