import { IPopupService, Popup, PopupPlacement } from "./IPopupService";
export declare class PopupService implements IPopupService {
    private container;
    private zindex;
    constructor(container: Element);
    showPopup(element: Element, popupPlacement?: PopupPlacement): Popup;
    closePopup(element: Element): void;
    bringToFront(element: Element): void;
}
