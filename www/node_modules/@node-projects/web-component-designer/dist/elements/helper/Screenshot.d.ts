export declare class Screenshot {
    private static _canvas;
    private static _context;
    private static _video;
    private static _captureStream;
    private static _disableStream;
    static get screenshotsEnabled(): boolean;
    static enableScreenshots(elementHostForVideo?: Element): Promise<void>;
    static takeScreenshot(element: Element, width?: number, height?: number, elementHostForVideo?: Element): Promise<string>;
    private static _sleep;
}
