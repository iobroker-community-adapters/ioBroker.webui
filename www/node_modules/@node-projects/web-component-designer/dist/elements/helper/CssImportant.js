export function splitCssImportant(value) {
    const match = value.match(/\s*!\s*important\s*$/i);
    if (!match)
        return { value, important: false };
    return { value: value.substring(0, match.index).trimEnd(), important: true };
}
export function appendCssImportant(value, important) {
    if (!important)
        return value;
    return value + ' !important';
}
//# sourceMappingURL=CssImportant.js.map