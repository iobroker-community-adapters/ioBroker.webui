import { IBinding } from '../../../dist/frontend/views/IBinding';

export class BindingHelper {
    
    binding: IBinding;

    constructor(binding: IBinding) {
        this.binding = binding;
    }
}