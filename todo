check for import state when npm install/update/remove

upload package.json in user config dir welche sim backup ist

images changed event ür solution explorer

runtime läd screen nicht neu bei save

custom event adding, inputbox where you can enter one